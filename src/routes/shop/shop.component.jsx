import './shop.styles.scss';

/**
 * Renders the Shop component.
 * @returns {JSX.Element} The rendered Shop component.
 */
const Shop = () => (
    <div>
        Welcome to the Shop
    </div>
);

export default Shop;